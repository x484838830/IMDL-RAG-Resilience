# 更新日誌 | Changelog

本專案的所有重要更改都將記錄在此檔案中。

---

## [5.5.0] - 2026-08-25

> 同期跨部門比較改版為「純粹並列比較」；全系統字級放大以適配桌面版；AI 助手 Q&A 可引用同頁的異常分析與完整報告。

### 🎉 同期跨部門比較改版（依莊秀文教授回饋）

- **一種計畫類型，一套視圖語言**：跨部門計畫不再借用「基準→最新」的跨期視圖。詳情頁依計畫類型切換；跨期流程零變更
- 頁首新增類型徽章：「同期跨部門比較 · 期別 · N 個部門」／「跨期比較 · 部門 · 期間」
- **01 概覽**：「韌性變化摘要」→「各部門韌性總覽」（每部門一卡：整體＋四潛能）；雷達圖改為 N 條線、一部門一色、圖例＝部門名；分數明細標題帶期別、列＝部門、純數字
- **02 潛能差異**：跨部門模式以分組長條圖取代折線趨勢圖
- **03 次指標**：跨部門版雷達（N 線）、部門 × 次指標表、「部門間落差最大的三項」「各部門最弱的一項」
- **純粹並列比較，沒有基準／標竿**：建立彈窗在同期跨部門選取時基準欄消失、不再要求指定；資料管理頁跨部門模式不顯示基準欄
- 部門顏色改為依部門鍵值固定（`deptColor`），HRI 頁與比較頁同一部門同一顏色
- 附 `docs/cross-department-comparison-redesign.md`（規劃、Apple 設計原則對應、實作紀錄、既有計畫標竿清理 SQL）

### ✨ 功能改進

- **AI 助手 Q&A 引用同頁產出**：自然語言查詢的 prompt 現在會附上已跑過的「異常分析」洞察清單與已生成的「完整分析報告」全文，並要求回答與其一致（數字仍以原始資料為準）；兩者都未產生時 prompt 與原本相同
- 「異常分析」結果改存於 `reportStore`（與報告相同），切換頁面回來仍在，Q&A 才引用得到
- **全系統字級放大**（因桌面版顯示偏小的回饋）：韌性能力比較主頁與計畫詳情頁、HRI 輸入／結果／Reference 頁、AI 報告頁（含報告正文 14.5 → 16.5px、側欄預設寬度 380 → 420）、問卷清單／編輯器／預覽彈窗——內文普遍 +2px、最小字級 13px 起、圖表刻度與圖例同步放大

---

## [5.4.0] - 2026-08-16

> 重大更新：HRI 重生為「波次計算」架構（同波次結構性保證＋涵蓋透明化）、新增 Reference 方法論證據頁（α／ICC／σ 全攤開）、兩種比較流程整併為單一入口，並修復跨使用者資料可見的重大資安問題與新增介面縮放功能。

### 🔒 安全性修復（重要）

- **修復登入使用者可看到其他人資料的問題**：根因為 8/11 的 RLS 加固腳本在 8 張表上新增了 `using (true)` 的全開政策，蓋過原本「只能存取自己資料」的 owner 政策（Postgres RLS 政策為任一滿足即放行）
  - 前端防線：追蹤計畫／分析紀錄／HRI 分析／問卷專案的所有查詢加上明確 `owner_id` 過濾（即使資料庫政策再出錯也不會顯示他人資料）
  - 資料庫治本：新增 `supabase/fix_rls_owner_scope.sql`（移除全開政策、冪等重建 8 張表的 owner 限定政策、維持 anon 無權限）——**需在 Supabase Dashboard → SQL Editor 手動執行一次**，並以兩個帳號交叉驗證

### 🎉 重大功能更新

#### 醫院韌性指數 HRI：波次計算架構
- 模組名稱定案回「**醫院韌性指數 HRI / Hospital Resilience Index (HRI)**」
- 新增「**依波次計算**」：從已儲存的分析紀錄依波次（時間窗）聚合計算全院 HRI；波次選擇器單選設計——**跨波次混算在結構上不存在**，而非算出來再警告
- 成立條件：紀錄需含原始逐人答案（rawJson）且為 5 點量尺（不符者不進計算）；同部門多筆取最新；可用部門少於 2 個顯示「無法聚合」（σ ddof=1 無法定義，不硬算）
- **涵蓋透明化取代門檻**：HRI 一律以正式身分顯示，數字旁強制並列「本波次涵蓋 N 個部門、M 份問卷」；代表性由讀者自行判斷，系統只負責把事實擺在旁邊
- 「**缺原始資料**」部門（有紀錄但無法使用）在熱區圖畫成灰色列明確標示——「有東西但不能用」與「根本沒測」是兩回事，不靜默消失
- 計算一律以 rawJson 重跑定案版 `computeHRI()`（個人→部門→全院四層等權平均），公式一行未動

#### Reference 方法論證據頁
- HRI 結果頁新增「**Reference 方法論證據**」入口——每個 HRI 數字帶著自己的正當性證明，依據與檢查結果攤在數字旁邊
- **A 方法依據層**（固定內容）：計算鏈每一步的方法選擇與依據（CTT 平均估計、ICC 聚合檢查、等權 μ＋σ 成對非補償性並陳、描述性輪廓重心定位、α／ICC／σ 品質配套）
- **B 本波次證據層**（隨波次動態計算，與 computeHRI 同一批原始答案）：
  - Cronbach's α：每部門 × 每潛能各一格，題數 < 4 掛「題數不足」旗標
  - ICC(1)/ICC(2) 聚合體檢（one-way ANOVA 式，組數不等以 n₀ 校正）分兩層：潛能層標「參考用」（各科題目不同含題組干擾）、共同題層標「可作準」（各部門對照表交集，同題同尺）；判讀門檻預先定死顯示於表上；部門 < 2 顯示「無法體檢」、交集為空顯示「本波次無共同題」
  - σ 旗標：四潛能中部門間 σ 最大者亮標（僅標最大，不設數值門檻）
- **判讀文案自動化**：主畫面熱區圖依「可作準」層 ICC(1) 結果掛三句固定文案（「部門差異經 ICC 檢驗為真」／「部門差異可能為個人雜訊，比較宜保守解讀」／「≈0（部門無差異）」），不做生成式發揮

#### 比較流程整併（單一入口）
- **資料總覽矩陣只存在於「韌性能力比較」頁**（HRI 頁移除）：橫列＝同部門跨期比較、縱欄＝同期跨部門比較，**兩者都就地開啟同一個「新增追蹤計畫」彈窗**，不再跳頁
- 同期跨部門選取時自動判定為 cross_department 類型，比較項目直接以**部門名稱**命名（隨語言翻譯），後續分析與跨期比較共用同一套計畫詳情頁
- 分數明細表移除重複的「Dept」欄（紀錄標籤本身已是部門名）
- 跨部門計畫若各部門次指標清單不一致，「韌性能力明細比較雷達圖」頁籤**整個隱藏**（避免不可比的雷達誤導）；跨期比較的偶發缺項則改為**交集雷達**＋琥珀色揭露提示，明細表保留聯集以「-」標缺

### ✨ 功能改進

- **介面縮放**：左下角帳號選單新增「縮放 − 100% ＋」（67%–150% 瀏覽器式檔位，點百分比還原），設定記憶於各台電腦；並以 `--zoom-vh` 補償機制修正放大後出現內外雙捲軸的問題（外殼、各頁面滿版高度、彈窗上限全面適配）
- **術語全面統一**：受訪者一律 respondent（棄用 recipient／interviewee／object）；潛能一律 Potential（全面棄用 dimension／維度）；整合醫學病房英文定案 **Hospital Medicine Ward**；次指標英文 Sub-indicator
- **模組更名**：RAG 韌性分析 → **RAG Analysis**（頁內標題採全名＋PRO）；Survey Builder 設定頁 → 問卷設計；Assessment Management → 問卷設計管理；登入前主畫面 → RAG Project Management
- **AI 措辭調整**：報告生成改為「重點洞察」框架（提示詞明令不使用「異常 anomaly」一詞描述數據）；自然語言查詢範例改用 Potential 用語；伺服器端 AI 提示詞同步改為 Potential 術語
- 追蹤模組與問卷邀請模組**殘留中英文全面清除**（英文介面不再出現中文、中文介面不再夾雜英文）

---

## [5.3.0] - 2026-08-15

> 介面優化版本：追蹤與 HRI 模組顯示全面強化（部門中英對照、雷達圖重整、資料總覽凍結欄＋拖曳）、RAG 分析頁新增部門欄位與量表警示，並修正 PDF 報告首頁標題跑版。

### ✨ 功能改進

#### RAG 韌性分析
- 「基本資訊」新增**部門**下拉選單（與「儲存至比較」面板同步連動，作為跨部門比較與全院 HRI 的自動配對依據）
- 「量表級距」下方新增警示訊息：提醒使用者確認問卷實際使用的量尺與此設定相同，否則分數與圖表將計算錯誤
- 修正 **PDF 報告首頁標題過長超出 A4**：標題自動換行、總分／摘要／菱形圖改為流動排版，每頁頁首過長時自動截斷加省略號

#### 韌性能力比較
- **資料總覽**：期別欄設定最小寬度且標籤禁止折行（不再擠壓跑版）；期別過多時改為水平**按住拖曳**平移，「部門」欄與「跨期比較」欄凍結不動
- 「新增追蹤計畫」彈窗**完整中英雙語化**（表單、紀錄表格、驗證訊息、刪除確認、自動產生的預設名稱）
- **差異摘要卡重新設計**：單行儀表板式排版（標題＋部門徽章＋數據靠左緊湊排列）、期別直接作為分數標籤（不再顯示 Baseline / Latest）、基準→最新之間加入流動箭頭動畫、Delta 改為 Change、卡片標題改為「整體韌性分數 / Overall Resilience Score」
- 「多維度疊加比較」更名「**整體韌性四潛能雷達比較**」：基準／最新改為**紅藍高對比**配色，圖例說明重寫（紅色實線＝基準期 · 藍色實線＝最新一期 · 灰色虛線＝其他歷史紀錄）
- 「四維度趨勢走向」更名「**韌性能力趨勢**」
- 次指標分析右欄新增「**韌性細部指標注意項目**」總標題；三份清單更名為「最弱的三項／進步最多的三項／退步最多的三項」（英文採完整寫法，不用縮略語）
- 新增計畫表格欄位「整體分數」更名「整體韌性分數」

#### 同時間不同部門比較（HRI）
- 側邊欄模組更名「**同時間不同部門比較**」（英文 Same-Period Cross-Department Comparison）
- 「四潛能剖面」更名「**四潛能雷達圖**」；新增「**各部門四潛能雷達比較**」——各部門以不同顏色疊加於同一張雷達圖，並附各部門平均值概念說明（部門分數＝受訪者個人分數的算術平均，全院 μ 部門等權）
- 跨部門分析表最後一欄「科剖面／診斷用」更名「**整體分數 / Overall Score**」

#### 中英文介面
- 部門名稱建立**顯示層中英對照**（資料庫儲存值不變，比較配對不受影響）：急診 Emergency Department、門診 Outpatient Department、手術 Operating Room、整合醫學病房 Integrated Medicine Ward 等，全面套用於資料總覽、比較模組、HRI 圖表與 RAG 分析下拉選單；英文一律使用全名不用縮寫
- 側邊欄「韌性能力比較」英文更名 **Resilience Potential Comparison**

---

## [5.2.0] - 2026-08-12

> 正式發布前的收尾版本：醫院韌性指數（HRI）方法學定案、PDF 報告圖表全面重製、追蹤模組中英文嚴格分離，以及打包前的全面資安加固與依賴漏洞清理。

### 🔒 安全性更新（打包發布前全面加固）
- 移除會回傳完整 Gemini API Key 的除錯端點 `/api/env-check`
- CORS 收緊為同源限定（`localhost:3000` / `127.0.0.1:3000`），阻止瀏覽器中其他網站隔空呼叫本機 API
- Electron `shell.openExternal` 加入協定白名單（僅允許 `http:` / `https:`），阻擋 `file://` 等協定外開
- `.env.local` 移出版控，`.gitignore` 新增 `.env*` 規則
- 依賴漏洞由 24 個降至 5 個：`react-router-dom` 升級 6.30.4（修 XSS 與開放重導向）、`xlsx` 改用 SheetJS 官方 0.20.3 修補版（修原型污染與 ReDoS）、`npm audit fix` 清理 qs / body-parser / ws 等（餘下 5 個經確認皆打不到本應用的使用情境）
- 新增 `supabase/tighten_rls.sql`：全表啟用 RLS、限定 authenticated、鎖死 anon 權限；`lib/supabase.ts` 內的 schema 文件同步改為收緊版政策

### 🎉 重大功能更新

#### 醫院韌性指數（HRI）方法學定案
- 計算方式定案為**四層算術平均**（題目 → 次指標 → 潛能 → 部門 → 全院），每層並列 μ／σ（樣本標準差 ddof=1）
- 移除權重、門檻、封頂等非補償式機制（z-score 降為輔助參考），指數定位為描述性輪廓
- 資料總覽（DataMatrix）新增**刪除分析紀錄**功能（懸停 × + 確認對話框）；操作提示改為「橫列＝同部門跨期比較；點選縱軸＝計算全院 HRI」
- 新增**建議比較組合**：依已載入資料自動推薦「同部門跨期」配對，一鍵帶入並顯示「已帶入」回饋
- 頁面重排：資料總覽移至最上方，上傳問卷資料移至最下方

#### 韌性能力比較（追蹤模組）整併與雙語化
- 「次指標雷達」與「次指標明細」合併為單一頁籤**「韌性能力明細比較雷達圖」**：明細表移至雷達圖下方、字體加大，無法比較時欄位自動收合
- **中英文嚴格分離**：中文介面不再夾雜英文（差異摘要卡、分數比較表、雷達疊圖、趨勢圖、判讀文字全面雙語化）
- 潛能名稱統一去「力」（回應力→回應、監測力→監測…）；「自基準」改為「基準」；「亟需關注」改為「極需關注」
- **設定基準更直覺**：資料管理表格的「基準」欄直接顯示「設為基準」按鈕，點一下即設定（原動作欄的星號圖示移除）
- **「追蹤類型」選項移除**：新增與編輯計畫改為自由填寫的「專案描述」欄位，比較一律採用標準的同部門跨期設計
- 依定位移除本模組全部 PDF 匯出功能

#### PDF 報告圖表重製
- 四潛能雷達圖與菱形圖以 **900×720 置中**重新繪製，改用 inline 樣式與 SVG 屬性（修正先前匯出時圖表變形、過小的問題）
- Excel 匯出改為打包內建 `xlsx` 模組，修正「未載入 Excel 匯出套件」錯誤

### ✨ 功能改進
- **Gemini 模型更新**：預設改為 `gemini-3.6-flash`，可選 3.5 Flash / 3.1 Pro / 3.5 Flash-Lite；舊模型 id 自動遷移（`migrateGeminiModelId` 單一來源）
- 主標題「RAG 韌性分析 PRO」改用漸層流動文字（尊重減少動態效果設定）；移除「RESILIENCE ANALYSIS • 離線模式」副標
- 問卷發送步驟列微動畫：連接線進度填滿、完成勾勾描邊、步驟切換高度彈簧過渡
- 帳號選單全面中文化（登入身分／設定／語言／升級方案／登出／訪客模式），移除選單內重複的「問卷管理」項目與側欄底部「v2.0 雲端版 (Beta)」字樣

### 🐛 錯誤修復
- 修正 Gmail 登入後出現「invalid request: both auth code and code verifier should be non-empty」的錯誤（授權碼被重複交換；AuthCallback 重寫，並修正錯誤參數未讀取、postMessage 目標過寬、逾時轉址計時器洩漏等問題）
- 修正差異摘要卡「最新()」標籤空白（id 比對錯誤）
- 修正明細雷達圖 tooltip「變化」永遠顯示「無」（以實際期別標籤取值）
- 修正分數比較表判讀句自相矛盾（同一潛能同時被列為「提升最明顯」與「變化較小」）
- 修正問卷發送步驟切換後內容空白（高度量測時機競態）
- 重建過期的 Electron 內嵌後端 bundle（`electron/serverApp.cjs`），與 `server.ts` 同步

---

## [5.1.0] - 2026-07-28

### ✏️ 名稱調整
- 模組「韌性能力追蹤」更名為「韌性能力比較」（側邊欄、頁面標題、RAG 韌性分析儲存設定等相關文字，中英文同步調整）
- 「次指標能力雷達」更名為「韌性能力明細比較雷達圖」
- 計畫詳情頁頁籤「分析管理」更名為「資料管理」

### ✨ 新功能
- 互動式 WebGL 粒子背景（`ParticleField`）：約 1,500–2,600 顆粒子隨流場漂移，滑鼠靠近排開＋漩渦、快速移動帶動粒子、點擊產生擴散脈衝
- 尊重「減少動態效果」系統設定（開啟時不載入粒子層）；不支援 WebGL 時自動退回靜態漸層背景；分頁切至背景自動暫停動畫

### 🐛 錯誤修正
- 修正粒子層在 React 開發模式（StrictMode 雙重掛載）下因 WebGL context 被銷毀而完全消失的問題
- 修正滑鼠掃過 UI 元素時粒子互動被 `pointerout` 事件反覆中斷的問題
- 修正「總體韌性評估」鑽石圖 SVG 格線的 `<path>` 語法錯誤（主控台不再出現 Expected moveto 錯誤）
- 移除多個頁面根節點的不透明底色（韌性能力比較、問卷建立、問卷管理、問卷發送、AI 報告產生），背景層得以正常顯示

---

## [5.0.0] - 2026-06-15

> 自 4.2.0 以來累積的重大版本：新增「問卷發送」「韌性能力比較」（當時名稱：韌性能力追蹤）「醫院韌性指數」三大模組、導入雲端帳號與資料同步，並以 Electron 重新推出 Windows 桌面安裝版。

### 🎉 重大功能更新

#### 問卷發送系統（Assessment Invitation）
- 四步驟精靈：基本資訊 → 收件名單 → 邀請信 → 確認寄送
- Excel / CSV 收件名單匯入，自動辨識欄位並逐筆驗證（錯誤 / 警告標示，可線上檢視、編輯與勾選）
- 邀請信編輯器 + 即時信件預覽，支援動態變數（收件者姓名、職稱、問卷名稱、問卷連結、截止日期、發送單位…）
- 經 Supabase Edge Function（Resend）批次寄送，可先寄「測試信」確認排版
- 信件內含「開始填答」按鈕與 QR Code

#### 韌性能力比較（Resilience Comparison，當時名稱：韌性能力追蹤）
- 三種追蹤型態：時間序列（time series）、跨部門（cross department）、前後對比（before / after）
- 以 Baseline 基準點串接多筆分析紀錄，建立追蹤計畫
- 視覺化：雷達疊圖、四能力與次指標雷達、差異摘要卡、分數比較表、維度趨勢圖、次指標總覽
- 資料一致性提醒：混合量表 / 版本、低樣本數（< 10）自動警示

#### 醫院韌性指數（HRRI）
- 全新頁面：以**關鍵性加權非補償式彙總**計算單一治理分數
- 計算層 `utils/hrri.ts`（純 TypeScript、完整型別）：標準化（Hollnagel 0–4 → 0–100，含正值地板 raw 0→20）、跨科加權幾何平均、CFC 封頂（最弱關鍵科低於門檻則全院封頂）
- 視覺化：頭條分數 + 四能力雷達 +（科 × 能力）熱區圖三件並陳，可切換健康 / 失效情境
- CFC 門檻設為 60（對應 Hollnagel raw 2.0 = Acceptable）

#### 雲端帳號與資料同步（Supabase）
- Google 帳號登入（OAuth）；`profiles` 個人資料、`survey_projects / survey_versions / survey_version_questions` schema，並以 RLS 做使用者資料隔離
- 「問卷管理」雲端化：草稿 / 發布 / 封存、搜尋、分頁、版本管理
- 公開問卷填答頁（Survey Public）：受訪者免登入填答、匿名 ID、裝置偵測，回覆寫入雲端
- 未登入 / 未設定雲端時，以 localStorage **Demo 模式**自動備援（`ProxyApiService` 動態切換）

#### 桌面版重新上線（Electron + NSIS 安裝程式）
- 重新提供 **Windows 桌面安裝版**：`IMDL RAG PRO Setup 5.0.0.exe`（NSIS 安裝精靈，桌面 / 開始選單捷徑）
- Electron 內嵌 Express 後端（API + 靜態 SPA），`electron-builder` 打包，鑽石 logo 圖示
- `server.ts` 重構為 `createApiApp()` / `createDesktopApp()`，桌面與網頁共用後端邏輯

#### 桌面版 Google 登入（OAuth）
- 桌面版改用**系統預設瀏覽器 + 本機 loopback** 完成 Google 登入，避開 Google 對內嵌瀏覽器的封鎖
- Supabase 改用 **PKCE flow**，授權碼以 `?code=` 帶回供 loopback 接收；網頁彈窗流程同樣相容

### ✨ 功能改進

#### 醫療照護次指標全面更新
- 依權威中英文總表（Excel）重建 Healthcare 次指標，共 **50 個唯一次指標**，涵蓋內科 / 急診 / 整合醫學病房 / 重症病房 / 門診五科
- 每個次指標含正確的中英文名稱、定義與科別歸屬；修正先前「預期」維度英文名稱錯位、移除重複次指標

#### 問卷發送修正
- 測試信改用名單中第一位真實收件者代入（原本寫死「王小明」）
- 即時預覽與實寄一致：姓名空白即顯示空白，不再以範例資料誤導

### 🐛 錯誤修復
- 修正 Express 5 不支援 `'*'` 萬用路由導致的伺服器啟動錯誤（改用 middleware fallback）
- 移除未使用且變數名不一致的死檔 `utils/email/emailTemplateRenderer.ts`

### ⚠️ 重大變更
- **重新提供 Windows 安裝版 (.exe)**（[4.2.0] 曾移除，本版以 Electron 重新導入）
- 導入**雲端帳號制**：雲端問卷管理 / 追蹤 / 發送需 Google 登入；未登入仍可用 Demo 模式本機試用
- Supabase 驗證流程由 implicit 改為 **PKCE**

---

## [4.2.0] - 2026-04-22

### 🎉 重大功能更新

#### 次指標系統（Sub-Indicators）
- **全新醫療場域次指標資料庫**：內建 80+ 個次指標，涵蓋 Response、Monitor、Anticipate、Learn 四大能力維度
- **部門篩選**：支援整合醫學病房（IMW）、急診（ED）、AMU、內科（IM）、門診（OPD）五個部門分類
- **中英雙語定義**：每個次指標附有完整英文 definition 與中文 definition_zh
- **新資料檔 `data/subIndicators.ts`**：統一管理次指標與部門選項，提供 `getSubIndicators()` 與 `getDepartments()` 工具函式

#### AI 批次出題
- 勾選次指標後點擊「AI 出題」，開啟設定 Modal
- 可自訂三個生成參數：受測對象（如「第一線現場人員」）、聚焦方向（如「實際執行情形」）、調查目的（選填）
- AI 根據次指標定義、產業、部門、Likert 量表標籤，批次生成 Likert 題目並自動排除已有題目的次指標
- 語言跟隨系統設定自動切換（繁中 / 英文）

#### 自動異常偵測
- 資料載入 Report Generator 後，自動呼叫 Gemini API 分析數據
- 偵測項目：高標準差指標（> 1.0）、低分維度、跨維度顯著差異
- 偵測結果以 Alert 提示卡列表方式顯示於頁面頂端
- 支援錯誤回退：503（模型過載）/ 404（模型不存在）/ API Key 無效各有對應提示訊息

#### AI 互動聊天（Report Generator）
- Report Generator 頁新增聊天介面，可針對當前分析資料與 AI 即時問答
- 對話記錄保存於 session，支援捲動至最新訊息
- 分析數據摘要自動作為 context 傳入每次對話

### ✨ 功能改進

#### 架構轉移：Electron → Express + Vite
- 移除 Electron、electron-builder、concurrently、cross-env、wait-on 相依套件
- 新增 `server.ts`：Express 5 後端伺服器，整合 Vite dev server（開發時 HMR 支援）
- 新增 cors、dotenv、express、tsx 相依套件
- 執行指令由 `npm run dev:web / dev:desktop` 統一改為 `npm run dev`（`tsx server.ts`）
- 移除 `electron/main.cjs` 與 electron-builder 打包設定

#### Results Report — 長條圖視覺化
- 新增四大潛能得分長條圖（BarChart），使用 Recharts BarChart + LabelList
- 各 bar 以對應潛能顏色填色，懸停顯示分數 tooltip

#### PDF 字體載入優化
- 中文字體改由 jsDelivr CDN（`cdn.jsdelivr.net/gh/googlefonts/noto-fonts`）動態載入
- 實作 `cachedFontBase64Promise` 全域快取，同一 session 僅下載一次（約 7MB）
- 移除舊版需要本地 `fonts/NotoSansTC-Regular.ttf` 的載入邏輯

#### AI 模型選項更新
- 新增可選模型：`gemini-3.1-pro-preview`、`gemini-3-flash`、`gemini-3.1-flash-lite`
- 新增繁中說明：最強推理能力 / 多模態理解 / 最低成本低延遲

#### Report Generator AI 分析強化
- 移除原有 `/api/ai/generate-report` API 呼叫，改為前端直接呼叫 Gemini SDK
- 強化分析提示詞：加入「摘要開頭 2-3 句」、「數字呈現更直覺（用排名 / 百分比）」、「高標準差先結論後列表」等輸出規範
- 嚴格禁止 AI 下結論、給改善建議、做因果推論

#### SurveyBuilder 重構
- 移除舊版 Question Bank（Supabase 來源）、Dimension 篩選、AI 助手 tab
- 移除 `INDUSTRY_PRIORITIES` 硬編碼常數
- 新增「清除所有題目」功能，需確認對話框才執行
- 修正 AI 產出語言：預設值隨系統語言切換同步更新

### 🐛 錯誤修復
- 修復 `DiamondChart` 未接收 `scale_points` prop 導致計算錯誤的問題
- 修復 `ResultsReport` 中 `useRef` 未正確清理的潛在記憶體問題
- 修復 `html2canvas-pro` → `html2canvas 1.4.1`（版本相容性問題）

### ⚠️ 重大變更
- **不再提供 Windows 安裝版 (.exe)**：架構已轉為網頁，需 Node.js 18+ 執行
- **SurveyBuilder 舊版 Question Bank 功能移除**：改由次指標系統取代
- **`/api/ai/generate-report` 端點移除**：AI 功能改為前端直接呼叫

---

## [4.1.0] - 2026-04-04

### 🎉 重大功能更新

#### 問卷建立器全面升級
- **專案式管理**：Survey 重命名為 Project，以 Project → Version 兩層結構管理問卷
- **版本生命週期**：Draft → Published → Archived 完整流程，Published 版本鎖定不可修改
- **版本複製**：可從現有版本建立新版本，保留題目快照

#### 題庫系統（Question Bank）
- 內建分類題庫（`data/questionBank.ts`）：20 題，涵蓋 General / Healthcare / Manufacturing / Technology / Finance 五個產業
- 依產業與維度篩選推薦題目，優先顯示符合當前專案產業的題目

#### AI 輔助出題
- 透過自然語言描述自動生成問卷題目（整合 Gemini AI）
- 支援按維度出題，生成結果可直接加入草稿版本

### ✨ 功能改進
- 版本管理介面：新增版本狀態標籤、發布 / 封存操作按鈕
- Share Link：Published 版本可複製公開連結，受訪者直接填答

### 🐛 錯誤修復
- 修復 `RadarCard` 量表上限永遠為預設值 5 的 bug（`scale_points` prop 未正確傳遞）
- 修復 AI 助手頁面移除後路由殘留問題

---

## [4.0.0] - 2026-03-17

### 🎉 重大功能更新

#### 中英文雙語介面（i18n）
- 全系統支援繁體中文 / 英文切換，包含所有按鈕、標籤、提示訊息、PDF 報告
- 語言設定持久化於 localStorage
- 新增 `locales/en.json` 與 `locales/zh.json` 翻譯檔，以 `useLanguage()` hook 存取

#### AI 助手（Google Gemini 整合）
- 內建 Gemini AI 助手頁面，支援自訂 API Key 與模型選擇（`gemini-2.0-flash` 等）
- `AISettingsContext` 管理 API Key、模型、設定視窗開關
- AI Settings Modal 獨立元件，可從任意頁面開啟

#### 動態背景效果
- `AnimatedBackground` 元件：漸層色塊浮動動畫 + 毛玻璃介面效果
- 各頁面背景主題色跟隨內容動態調整

### ✨ 功能改進
- 雷達圖標籤直接顯示分數（不需懸停 tooltip）
- 新增 3 點與 6 點量表模板，移除 10 點量表
- 配置檔支援中文 Potential 名稱（回應、監測、預期、學習）

---

## [3.2.0] - 2026-02-08

### ✨ 功能改進

#### 圖表外觀自訂
- 可選擇 5 種主題色票（Default / Ocean / Forest / Sunset / Mono）
- 調整雷達圖線條粗細（1–5px）與填色透明度
- 自訂設定即時套用，無需重新分析

#### 匯出功能強化
- 新增 Excel 匯出：一鍵匯出含統計摘要的 Excel 檔案
- 組織名稱輸入欄位，顯示於 PDF 標題與 Excel 頁首

#### 表格操作
- 結果表格支援點擊標題欄位排序（升冪 / 降冪）
- 可快速找出各 Potential 中的弱項指標

### 🐛 錯誤修復
- 修復自訂顏色配置（`Colors` 工作表）無法正確讀取的問題

---

## [3.1.0] - 2026-01-28

### ✨ 功能改進

#### 完整中文介面
- 所有按鈕、標籤、提示訊息改為繁體中文
- 匯出的 PDF 報告完整支援中文顯示
- 嵌入 Noto Sans TC 字體至應用程式包（7.1MB），解決中文 PDF 亂碼問題

#### 配置檔中英文相容
- `Question_Mapping` 工作表 Potential 欄位可使用中文（回應、監控、預見、學習）或英文

---

## [3.0.0] - 2026-01-20

### 🎉 重大功能更新

#### PDF 匯出功能全面革新
- **完整重新設計**：採用混合式方法（html2canvas + jspdf-autotable）
- **多頁面配置**：
  - 第 1 頁：執行摘要（菱形圖 + 關鍵潛能分數）
  - 第 2–5 頁：個別潛能詳細分析（含雷達圖）
  - 後續頁：完整統計表格（自動分頁）
- **視覺強化**：顏色編碼指標、一致頁首 / 頁尾、日期與頁碼

### ✨ 功能改進
- **三級績效分類**：🔴 需要改進（< 3.0）/ 🟠 中等表現（3.0–3.9）/ 🟢 表現良好（≥ 4.0）
- **資料品質警示**：自動偵測未定義回答選項，於報告首頁顯示黃色警告卡片
- **智慧分頁**：表格自動分頁，避免內容截斷

### 🐛 錯誤修復
- 修復表格在分頁處被切斷的問題
- 解決 PDF 文字渲染的字元編碼問題

### ⚠️ 重大變更
- PDF 結構完全重組（從單頁截圖改為多頁專業報告）
- 評分標準從二級制改為三級制

---

## [2.0.0] - 2026-01-15

### 🎉 重大功能更新

#### Windows 桌面應用程式
- 新增獨立 Windows 安裝檔（.exe），使用 Electron 框架打包
- 支援完全離線使用，無需安裝 Node.js

#### 資料警告系統
- 自動偵測並顯示無法匹配的回答選項
- 無法識別的回答排除計算，介面顯示黃色警告區塊

### ✨ 功能改進
- 計算邏輯完全依賴設定檔（移除所有寫死關鍵字）
- Likert 分數匹配改為不區分大小寫

### 🐛 錯誤修復
- 修復部分回答選項無法正確識別的問題
- 修復雷達圖 Tooltip 顯示 "1.1 - 1.1.Event List" 重複編號的問題

---

## [1.0.0] - 2026-01-02

### 🎉 首次發布

#### 核心功能
- 問卷資料分析：支援上傳 Excel 格式問卷資料
- 配置檔系統：透過 Excel 配置檔定義 Likert 對應與題目結構
- 四個 Potential 分析：Response / Monitor / Anticipate / Learn

#### 視覺化功能
- 雷達圖（每個 Potential 獨立呈現）
- 菱形圖（四個 Potential 面積占比與整體韌性分數）
- 統計表格（平均分數、標準差等）

#### 報告匯出
- PDF 匯出：一鍵下載完整分析報告

---

## 更新類型圖示

| 圖示 | 說明 |
|------|------|
| 🎉 | 重大功能更新 |
| ✨ | 功能改進 |
| 🐛 | 錯誤修復 |
| 🔧 | 技術細節 |
| ⚠️ | 重大變更 |
| 🔒 | 安全性更新 |

---

## 問題回報

發現 Bug 或有功能建議？歡迎在 [GitHub Issues](https://github.com/x484838830/RAG-Resilience-Analyzer/issues) 提出！
